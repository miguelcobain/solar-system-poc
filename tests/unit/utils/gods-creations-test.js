import godsCreations from 'solar-system-poc/utils/gods-creations';
import { module, test } from 'qunit';

module('Unit | Utility | gods creations');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = godsCreations();
  assert.ok(result);
});
