
import { classify } from 'solar-system-poc/helpers/classify';
import { module, test } from 'qunit';

module('Unit | Helper | classify');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classify([42]);
  assert.ok(result);
});

